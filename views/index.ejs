<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Verge Articles</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
            max-width: 800px;
            margin: 0 auto;
            padding: 40px 20px;
            line-height: 1.6;
            color: #333;
            background: #fafafa;
        }

        h1 {
            color: #111;
            margin-bottom: 40px;
            font-size: 28px;
            font-weight: 600;
        }

        .article {
            background: white;
            margin-bottom: 16px;
            padding: 20px;
            border-radius: 8px;
            border: 1px solid #eee;
            transition: all 0.2s ease;
        }

        .article-title {
            color: #111;
            text-decoration: none;
            font-size: 18px;
            font-weight: 500;
            display: block;
            margin-bottom: 8px;
        }

        .article-date {
            color: #666;
            font-size: 14px;
            margin: 0;
        }
    </style>
</head>
<body>
    <h1>Articles from The Verge</h1>
    
    <% articles.forEach(article => { %>
        <div class="article">
            <a href="https://www.theverge.com<%= article.link %>" target="_blank" class="article-title">
                <%= article.title %>
            </a>
            <p class="article-date"><%= new Date(article.date).toLocaleDateString('en-US', { 
                year: 'numeric', 
                month: 'long', 
                day: 'numeric' 
            }) %></p>
        </div>
    <% }) %>
</body>
</html>